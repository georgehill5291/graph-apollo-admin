mutation CreateProduct($createProductInput: CreateProductInput!, $productImage: Upload!) {
    createProduct(createProductInput: $createProductInput, productImage: $productImage) {
        code
        success
        message
        product {
            title
            desc
            img
            price
        }
    }
}
