{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "attach",
            "name": "Attach to Chrome 1 ",
            "port": 9222,
            "webRoot": "${workspaceFolder}"
        }
    ]
}
